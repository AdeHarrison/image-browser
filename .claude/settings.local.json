{
  "permissions": {
    "allow": [
      "Bash(mvn -q verify)",
      "Bash(mvn verify *)",
      "Bash(mvn -q -DskipTests package)",
      "Bash(ls -la spreadsheet.xlsx)",
      "Bash(ls -la images.db*)"
    ]
  }
}
